

import 'package:flutter/cupertino.dart';

class ExceriseTile extends StatelessWidget {
  const ExceriseTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
