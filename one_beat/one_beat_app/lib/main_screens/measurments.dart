

import 'package:flutter/cupertino.dart';

class measurments extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("measumrent")),
    );
  }
}
